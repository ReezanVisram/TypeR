import { ProductInformationLayoutProps } from './types';

const usePresenter = (
    props: ProductInformationLayoutProps
): ProductInformationLayoutProps => {
    return props;
};

export default usePresenter;
