import { formatDate } from '../../../lib/utils';
import { PriceGraphProps } from './types';

const usePresenter = (props: PriceGraphProps): PriceGraphProps => {
    return props;
};

export default usePresenter;
